# inventory.example.ini
#
# Copie este arquivo para inventory.ini (NAO versionado, ver .gitignore)
# e ajuste os hosts/IPs reais do seu ambiente.

[rhel9_servers]
server01.example.local
server02.example.local
server03.example.local

# Voce tambem pode usar IPs diretamente:
# 10.0.0.11
# 10.0.0.12

[rhel9_servers:vars]
ansible_user=seu_usuario_sudo
# ansible_ssh_private_key_file=~/.ssh/id_rsa_ansible
